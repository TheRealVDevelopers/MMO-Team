rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: ALLOW ALL ACCESS
    // This completely bypasses authentication for debugging
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
