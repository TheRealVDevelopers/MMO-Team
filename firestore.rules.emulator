rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // EMULATOR ONLY - Permissive rules for Phase 7 testing
    // WARNING: NEVER deploy these to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
